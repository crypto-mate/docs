---
title: "Create virtual card"
description: "Create a new virtual card with limits and cardholder information."
api: "POST /cards/virtual-cards/create"
---

Creates a new virtual card with the specified limits and cardholder information. Requires an API key with access level 2 or higher.

## Body

<ParamField body="card_holder_name" type="string" required>
  Name of the cardholder. The card issuer prints it on the card in uppercase, so the casing you send is not preserved on the plastic.

  **Accepted characters:** letters (`A-Z`, `a-z`), digits (`0-9`), the letters `äöüÄÖÜ`, space, and `/` `-` `^` `.` `'` `)`. Note the right parenthesis is accepted and the left one is not.

  **Maximum length:** 26 characters, measured after the normalization below. A longer name is rejected — it is never silently truncated.

  **Normalization applied before the card is issued:** leading and trailing whitespace is trimmed, and any accented letter that has a close ASCII equivalent is transliterated — `José` is issued and stored as `Jose`, `Łukasz` as `Lukasz`. The letters `äöüÄÖÜ` are an exception and reach the issuer untouched. Internal spacing is left exactly as you send it.

  Anything still outside the accepted set after that normalization is rejected with `400` — this includes `ß`, `Ø`, `Æ`, and any non-Latin script such as Cyrillic or CJK, none of which have an ASCII equivalent to fall back on.

  Read the value back from `card_holder_name` in the response to see what was actually stored.
</ParamField>

<ParamField body="email" type="string">
  Email address for card notifications.
</ParamField>

<ParamField body="approval_method" type="string" required>
  Transaction approval method. Possible values: `WEBHOOK`, `TOPUP`, `DEFI`.
</ParamField>

<ParamField body="phone_country_code" type="integer" required>
  Phone country code for OTP authentication.
</ParamField>

<ParamField body="phone" type="string" required>
  Phone number for OTP authentication (without country code).
</ParamField>

<ParamField body="daily_limit" type="number">
  Daily spending limit in USD.
</ParamField>

<ParamField body="weekly_limit" type="number">
  Weekly spending limit in USD.
</ParamField>

<ParamField body="monthly_limit" type="number">
  Monthly spending limit in USD.
</ParamField>

<ParamField body="per_transaction_limit" type="number">
  Maximum amount allowed for a single purchase in USD, fees included. Does not apply to ATM withdrawals.
</ParamField>

<ParamField body="metadata" type="object">
  Additional metadata key-value pairs for the card.

  Some keys are reserved by the platform and you cannot set them here. If the metadata carries a reserved key, the call answers `412` and no card is created. The `message` names the key that was rejected.
</ParamField>

## Response

<ResponseField name="id" type="string">
  Unique card identifier.
</ResponseField>

<ResponseField name="card_holder_name" type="string">
  Name of the cardholder.
</ResponseField>

<ResponseField name="type" type="string">
  Card type.
</ResponseField>

<ResponseField name="last4" type="string">
  Last 4 digits of the card number.
</ResponseField>

<ResponseField name="status" type="string">
  Current card status.
</ResponseField>

<ResponseField name="approval_method" type="string">
  Transaction approval method.
</ResponseField>

<ResponseField name="daily_limit" type="number">
  Daily spending limit in USD.
</ResponseField>

<ResponseField name="weekly_limit" type="number">
  Weekly spending limit in USD.
</ResponseField>

<ResponseField name="monthly_limit" type="number">
  Monthly spending limit in USD.
</ResponseField>

<ResponseField name="per_transaction_limit" type="number">
  Maximum amount allowed for a single purchase in USD. `null` when there is no per-transaction limit.
</ResponseField>

<RequestExample>

```bash cURL
curl -X POST "https://api.cryptomate.me/cards/virtual-cards/create" \
  -H "x-api-key: $CRYPTOMATE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "card_holder_name": "John Doe",
    "email": "cardholder@example.com",
    "approval_method": "TOPUP",
    "phone_country_code": 1,
    "phone": "5551234567",
    "daily_limit": 1000.00,
    "weekly_limit": 5000.00,
    "monthly_limit": 20000.00,
    "per_transaction_limit": 500.00,
    "metadata": {
      "department": "engineering"
    }
  }'
```

</RequestExample>

<ResponseExample>

```json 200 OK
{
  "id": "card_6f1e2a30-4a3c-4b2e-9a1d-5e8c9b7a1f23",
  "card_holder_name": "John Doe",
  "type": "VIRTUAL",
  "last4": "4242",
  "status": "ACTIVE",
  "approval_method": "TOPUP",
  "daily_limit": 1000.00,
  "weekly_limit": 5000.00,
  "monthly_limit": 20000.00,
  "per_transaction_limit": 500.00
}
```

```json 400 Bad Request
{
  "code": "BAD_REQUEST",
  "message": "card_holder_name accepts letters (A-Z, a-z), digits (0-9), the letters äöüÄÖÜ, space, and / - ^ . apostrophe and right parenthesis, up to 26 characters. Other accented letters are converted to their closest ASCII equivalent; anything left outside that set is rejected"
}
```

```json 412 Precondition Failed
{
  "code": "VAL",
  "message": "The current limit for this product has been reached. Please increase the limit."
}
```

```json 412 Reserved Metadata Key
{
  "code": "VAL",
  "message": "The metadata key '{key}' is reserved by the platform and cannot be set on card creation."
}
```

</ResponseExample>
