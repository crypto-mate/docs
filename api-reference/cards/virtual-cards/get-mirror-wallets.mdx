---
title: "Get card mirror wallets"
description: "List the mirror wallet addresses of a virtual card."
api: "GET /cards/virtual-cards/{cardId}/mirror-wallets"
---

<Note>
  **A mirror wallet accepts USDC only.** Any other asset is rejected by the network, so it never leaves the wallet you sent it from and no funds are lost.

  A USDC deposit at a mirror address is credited to the card balance, the same as a deposit at the address returned by [Get card top-up wallets](/api-reference/cards/virtual-cards/get-card-top-up-wallets).
</Note>

Returns the mirror wallets of the card: the additional inbound addresses it has on other blockchains, each of which funds the card balance. The card's main deposit address is not a mirror wallet and is not part of this list — it comes from [Get card top-up wallets](/api-reference/cards/virtual-cards/get-card-top-up-wallets). Requires an API key with access level 1 or higher.

A card that has no mirror wallet answers `200` with an empty list. Only Stellar is available today, so a card has at most one mirror wallet.

Individual cards have the same pair of endpoints — see [Get individual card mirror wallets](/api-reference/cards/individual-cards/get-individual-card-mirror-wallets).

## Path parameters

<ParamField path="cardId" type="string" required>
  Card identifier.
</ParamField>

## Response

Returns an array of mirror wallet objects.

<ResponseField name="address" type="string">
  Inbound address of the mirror wallet on its blockchain.
</ResponseField>

<ResponseField name="blockchain" type="string">
  Blockchain of the mirror wallet. Always `STELLAR` today.
</ResponseField>

<ResponseField name="enabled" type="boolean">
  Whether the mirror wallet is receiving deposits. A new mirror wallet comes back `true`. A `false` means the address is not accepting deposits — do not send funds to it while it reads `false`. This is not a state you can change.
</ResponseField>

<RequestExample>

```bash cURL
curl -X GET "https://api.cryptomate.me/cards/virtual-cards/card_6f1e2a30-4a3c-4b2e-9a1d-5e8c9b7a1f23/mirror-wallets" \
  -H "x-api-key: $CRYPTOMATE_API_KEY"
```

</RequestExample>

<ResponseExample>

```json 200 OK
[
  {
    "address": "GDQNY3PBOJOKYZSRMK2S7LHHGWZIUISD4QORETLMXEWUVBVBDOOWMGF5",
    "blockchain": "STELLAR",
    "enabled": true
  }
]
```

```json 200 No Mirror Wallets
[]
```

```json 404 Not Found
{
  "code": "NOT_FOUND",
  "message": "Card not found"
}
```

</ResponseExample>
