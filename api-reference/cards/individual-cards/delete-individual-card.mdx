---
title: "Delete individual card"
description: "Permanently delete an individual card."
api: "DELETE /cards/individual-cards/{cardId}"
---

Permanently deletes an individual card. The deletion is irreversible: the card stops authorizing transactions and cannot be reactivated. Deleting a card frees one renewable slot of your company's card plan, so you can issue another individual card in its place. A card that still holds a virtual balance cannot be deleted: the request answers `412` and the card stays as it is. Any positive amount blocks the deletion, however small; only a balance of exactly zero deletes. Cards whose `approval_method` is not `TOPUP` have no virtual balance and are never blocked. Requires an API key with access level 2 or higher.

## Path parameters

<ParamField path="cardId" type="string" required>
  Unique identifier of the individual card.
</ParamField>

## Response

Returns the card with its final state.

<ResponseField name="id" type="string">
  Unique card identifier.
</ResponseField>

<ResponseField name="rain_card_id" type="string">
  Rain provider card identifier.
</ResponseField>

<ResponseField name="client_id" type="string">
  ID of the individual client the card belongs to.
</ResponseField>

<ResponseField name="type" type="string">
  Card type.
</ResponseField>

<ResponseField name="status" type="string">
  Final card status. `DELETED` after a successful deletion.
</ResponseField>

<ResponseField name="last4" type="string">
  Last 4 digits of the card number.
</ResponseField>

<ResponseField name="expiration_month" type="integer">
  Card expiration month.
</ResponseField>

<ResponseField name="expiration_year" type="integer">
  Card expiration year.
</ResponseField>

<ResponseField name="card_holder_name" type="string">
  Name printed on the card.
</ResponseField>

<ResponseField name="approval_method" type="string">
  Transaction approval method.
</ResponseField>

<ResponseField name="daily_limit" type="number">
  Daily spending limit in USD.
</ResponseField>

<ResponseField name="weekly_limit" type="number">
  Weekly spending limit in USD.
</ResponseField>

<ResponseField name="monthly_limit" type="number">
  Monthly spending limit in USD.
</ResponseField>

<ResponseField name="per_transaction_limit" type="number">
  Maximum amount allowed for a single purchase in USD. `null` when there is no per-transaction limit.
</ResponseField>

<ResponseField name="forwarded_3ds_type" type="string">
  3DS challenge delivery method. Possible values: `sms`, `webhook`.
</ResponseField>

<RequestExample>

```bash cURL
curl -X DELETE "https://api.cryptomate.me/cards/individual-cards/icard_6f1e2a30-4a3c-4b2e-9a1d-5e8c9b7a1f23" \
  -H "x-api-key: $CRYPTOMATE_API_KEY"
```

</RequestExample>

<ResponseExample>

```json 200 OK
{
  "id": "icard_6f1e2a30-4a3c-4b2e-9a1d-5e8c9b7a1f23",
  "rain_card_id": "rain_9d3b2c10-1a2b-3c4d-5e6f-7a8b9c0d1e2f",
  "client_id": "client_abc123",
  "type": "Virtual",
  "status": "DELETED",
  "last4": "4242",
  "expiration_month": 12,
  "expiration_year": 2028,
  "card_holder_name": "JOHN DOE",
  "approval_method": "TOPUP",
  "daily_limit": 100.00,
  "weekly_limit": 500.00,
  "monthly_limit": 2000.00,
  "per_transaction_limit": 50.00,
  "forwarded_3ds_type": "sms"
}
```

```json 404 Not Found
{
  "code": "NOT_FOUND",
  "message": "Card not found"
}
```

```json 412 Has Balance
{
  "code": "VAL",
  "message": "Cannot delete card TLyVxTwALL9ClB7QEIW4HEttxeSI5OzB. It still has a virtual balance of 3.50."
}
```

</ResponseExample>
